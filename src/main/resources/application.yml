server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

spring:
  messages:
    basename: messages
  security:
    user:
      name: taemin
      password: rla2647

management:
  endpoints:
    web:
      exposure:
        include: "*"

